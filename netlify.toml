[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback lives in public/_redirects. The ideas function is served from
# the reserved /.netlify/functions/ namespace, which is matched before that
# catch-all, so it is never shadowed.
